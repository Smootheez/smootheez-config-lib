{
  "schemaVersion": 1,
  "id": "${modid}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${mod_desc}",
  "authors": [
    "Smootheez"
  ],
  "license": "${mod_license}",
  "environment": "client",
  "entrypoints": {
    "client": [
      "net.smootheez.scl.Scl"
    ],
    "modmenu": [
      "net.smootheez.scl.modmenu.ModMenuImpl"
    ]
  },
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric-api": "*",
    "minecraft": ">=${minecraft_version}"
  }
}