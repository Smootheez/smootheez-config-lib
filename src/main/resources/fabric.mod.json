{
  "schemaVersion": 1,
  "id": "${modid}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${mod_desc}",
  "authors": [
    "Smootheez"
  ],
  "license": "${mod_license}",
  "icon": "assets/scl/icon.png",
  "environment": "client",
  "entrypoints": {
    "client": [
      "dev.smootheez.scl.Scl"
    ]
  },
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "~${minecraft_version}"
  }
}